#include "main.h"

/**
 * _abs - compute the absolute value of integer
 * @i: integer to be conveted
 * main - check the code
 * Return: absolute value of input number
 */
int _abs(int i)
{
	if (i < 0)
		i = i * -1;
}
int main(void)
{
	return (i);
}
